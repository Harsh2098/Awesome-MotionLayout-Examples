<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hmproductions.motional">

    <queries>
        <package android:name="com.hmproductions.sgbuses" />
        <package android:name="com.hmproductions.sgbuses.debug" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Motional">

        <activity
            android:name=".GeometryActivity"
            android:label="Geometry Dash"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".BonusActivity"
            android:label="Bonus"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GalleryActivity"
            android:label="Gallery"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MagnetActivity"
            android:label="Magnets"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>